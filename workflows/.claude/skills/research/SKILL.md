---
name: research
description: Codebase exploration, convention detection, file mapping with file:line evidence. Use after intake.
---

# research

**ZA-Assessments-Portal-FE** — React 18 + Vite SPA, JavaScript/JSX, `@/` alias.

## Conventions to verify

- API in `src/api/`, React Query + Redux
- Auth: `useAuth()` + LTI; Sonner toasts; Tailwind v3
- Flag proctoring/security impact if applicable

## Procedure

1. Read AGENTS.md + CLAUDE.md
2. Search with 3+ synonyms
3. Trace async lifecycle
4. Verify nearby conventions
5. npm run lint; validate hypotheses

## Output

Conventions, files to change (file:line), reference files, hypotheses, open questions.
